# 📊 System Flowcharts

## 1. User Signup Flow

```
┌─────────────┐
│   Client    │
│  (Browser)  │
└──────┬──────┘
       │
       │ POST /api/signup
       │ {email, password, name}
       ▼
┌─────────────────────┐
│   Flask API         │
│   (api.py)          │
└──────┬──────────────┘
       │
       │ 1. Validate input
       ▼
┌─────────────────────┐
│  Check if user      │
│  already exists     │
└──────┬──────────────┘
       │
       ├─ Yes ──► Return 400 "User already exists"
       │
       │ No
       ▼
┌─────────────────────┐
│  Hash password      │
│  (bcrypt)           │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Store in MongoDB   │
│  users collection   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Generate JWT       │
│  token              │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Return 201         │
│  {token, user}      │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   Client    │
│ (Save token)│
└─────────────┘
```

---

## 2. User Login Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ POST /api/login
       │ {email, password}
       ▼
┌─────────────────────┐
│   Flask API         │
└──────┬──────────────┘
       │
       │ 1. Find user by email
       ▼
┌─────────────────────┐
│  Query MongoDB      │
│  users collection   │
└──────┬──────────────┘
       │
       ├─ Not found ──► Return 401 "Invalid credentials"
       │
       │ Found
       ▼
┌─────────────────────┐
│  Verify password    │
│  bcrypt.checkpw()   │
└──────┬──────────────┘
       │
       ├─ Invalid ──► Return 401 "Invalid credentials"
       │
       │ Valid
       ▼
┌─────────────────────┐
│  Check active       │
│  status             │
└──────┬──────────────┘
       │
       ├─ Inactive ──► Return 403 "Account disabled"
       │
       │ Active
       ▼
┌─────────────────────┐
│  Generate JWT       │
│  token              │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Return 200         │
│  {token, user}      │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   Client    │
│ (Save token)│
└─────────────┘
```

---

## 3. Authenticated Request Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ GET /api/me
       │ Authorization: Bearer <token>
       ▼
┌─────────────────────┐
│   Flask API         │
│   @token_required   │
└──────┬──────────────┘
       │
       │ 1. Extract token from header
       ▼
┌─────────────────────┐
│  Token present?     │
└──────┬──────────────┘
       │
       ├─ No ──► Return 401 "Token missing"
       │
       │ Yes
       ▼
┌─────────────────────┐
│  Decode JWT token   │
│  jwt.decode()       │
└──────┬──────────────┘
       │
       ├─ Invalid/Expired ──► Return 401 "Invalid token"
       │
       │ Valid
       ▼
┌─────────────────────┐
│  Extract user info  │
│  {user_id, email,   │
│   is_admin}         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Attach to request  │
│  request.user = data│
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Process request    │
│  (get user profile) │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Return 200         │
│  {user data}        │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   Client    │
└─────────────┘
```

---

## 4. Admin CRUD Flow (Update User)

```
┌─────────────┐
│   Admin     │
│   Client    │
└──────┬──────┘
       │
       │ PUT /api/admin/users/:id
       │ Authorization: Bearer <admin_token>
       │ {password: "newpass123"}
       ▼
┌─────────────────────┐
│   Flask API         │
│   @token_required   │
│   @admin_required   │
└──────┬──────────────┘
       │
       │ 1. Validate token
       ▼
┌─────────────────────┐
│  Token valid?       │
└──────┬──────────────┘
       │
       ├─ No ──► Return 401 "Invalid token"
       │
       │ Yes
       ▼
┌─────────────────────┐
│  Check is_admin     │
│  flag               │
└──────┬──────────────┘
       │
       ├─ No ──► Return 403 "Admin access required"
       │
       │ Yes (is admin)
       ▼
┌─────────────────────┐
│  Hash new password  │
│  bcrypt.hashpw()    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Update MongoDB     │
│  users collection   │
└──────┬──────────────┘
       │
       ├─ User not found ──► Return 404 "User not found"
       │
       │ Updated
       ▼
┌─────────────────────┐
│  Return 200         │
│  {message: "User    │
│   updated"}         │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   Admin     │
│   Client    │
└─────────────┘
```

---

## 5. AI Chat Flow

```
┌─────────────┐
│   User      │
│   Client    │
└──────┬──────┘
       │
       │ POST /api/chat
       │ Authorization: Bearer <token>
       │ {question, chart_data, niche}
       ▼
┌─────────────────────┐
│   Flask API         │
│   @token_required   │
└──────┬──────────────┘
       │
       │ 1. Validate token
       ▼
┌─────────────────────┐
│  Token valid?       │
└──────┬──────────────┘
       │
       ├─ No ──► Return 401 "Invalid token"
       │
       │ Yes
       ▼
┌─────────────────────┐
│  Extract user_id    │
│  from token         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Parse chart data   │
│  ChartParser        │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Extract factors    │
│  {7th_house,        │
│   venus_sign, ...}  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Call AI            │
│  Orchestrator       │
└──────┬──────────────┘
       │
       ├──► Generate embeddings (Gemini)
       │
       ├──► Retrieve from RAG (Vertex AI)
       │
       ├──► Synthesize response (GPT-4.1 Mini)
       │
       ▼
┌─────────────────────┐
│  AI Response        │
│  generated          │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Store in MongoDB   │
│  chats collection   │
│  {user_id,          │
│   question,         │
│   response, ...}    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Return 200         │
│  {response,         │
│   complexity,       │
│   passages_used}    │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   User      │
│   Client    │
└─────────────┘
```

---

## 6. Admin Delete User Flow

```
┌─────────────┐
│   Admin     │
└──────┬──────┘
       │
       │ DELETE /api/admin/users/:id
       │ Authorization: Bearer <admin_token>
       ▼
┌─────────────────────┐
│   Flask API         │
│   @token_required   │
│   @admin_required   │
└──────┬──────────────┘
       │
       │ 1. Validate admin token
       ▼
┌─────────────────────┐
│  Is admin?          │
└──────┬──────────────┘
       │
       ├─ No ──► Return 403 "Admin access required"
       │
       │ Yes
       ▼
┌─────────────────────┐
│  Delete user from   │
│  users collection   │
└──────┬──────────────┘
       │
       ├─ Not found ──► Return 404 "User not found"
       │
       │ Deleted
       ▼
┌─────────────────────┐
│  Delete all chats   │
│  for this user_id   │
│  from chats         │
│  collection         │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Return 200         │
│  {message: "User    │
│   deleted"}         │
└──────┬──────────────┘
       │
       ▼
┌─────────────┐
│   Admin     │
└─────────────┘
```

---

## 7. Complete User Journey

```
┌──────────────────────────────────────────────────────────┐
│                    NEW USER JOURNEY                       │
└──────────────────────────────────────────────────────────┘

Step 1: Signup
┌─────────┐
│ Client  │ ──► POST /api/signup ──► Get token
└─────────┘

Step 2: Login (future sessions)
┌─────────┐
│ Client  │ ──► POST /api/login ──► Get token
└─────────┘

Step 3: View Profile
┌─────────┐
│ Client  │ ──► GET /api/me ──► See profile
└─────────┘      (with token)

Step 4: Ask AI Question
┌─────────┐
│ Client  │ ──► POST /api/chat ──► Get AI response
└─────────┘      (with token)

Step 5: View History
┌─────────┐
│ Client  │ ──► GET /api/chat/history ──► See past chats
└─────────┘      (with token)
```

---

## 8. Complete Admin Journey

```
┌──────────────────────────────────────────────────────────┐
│                    ADMIN JOURNEY                          │
└──────────────────────────────────────────────────────────┘

Step 1: Login as Admin
┌─────────┐
│ Admin   │ ──► POST /api/login ──► Get admin token
└─────────┘      (admin credentials)

Step 2: View All Users
┌─────────┐
│ Admin   │ ──► GET /api/admin/users ──► See all users
└─────────┘      (with admin token)

Step 3: View User Details (with password)
┌─────────┐
│ Admin   │ ──► GET /api/admin/users/:id ──► See user + password
└─────────┘      (with admin token)

Step 4: Create New User
┌─────────┐
│ Admin   │ ──► POST /api/admin/users ──► User created
└─────────┘      (with admin token)

Step 5: Update User Password
┌─────────┐
│ Admin   │ ──► PUT /api/admin/users/:id ──► Password updated
└─────────┘      (with admin token)

Step 6: Delete User
┌─────────┐
│ Admin   │ ──► DELETE /api/admin/users/:id ──► User deleted
└─────────┘      (with admin token)
```

---

## 9. Token Lifecycle

```
┌─────────────────────────────────────────────────────────┐
│                   TOKEN LIFECYCLE                        │
└─────────────────────────────────────────────────────────┘

1. User logs in
   │
   ▼
2. Server generates JWT token
   │
   ├─ Payload: {user_id, email, is_admin, exp}
   ├─ Sign with SECRET_KEY
   └─ Set expiry: 24 hours
   │
   ▼
3. Return token to client
   │
   ▼
4. Client stores token
   │
   ├─ localStorage
   ├─ sessionStorage
   └─ Cookie
   │
   ▼
5. Client includes token in requests
   │
   └─ Authorization: Bearer <token>
   │
   ▼
6. Server validates token
   │
   ├─ Verify signature
   ├─ Check expiry
   └─ Extract payload
   │
   ├─ Valid ──► Process request
   │
   └─ Invalid/Expired ──► Return 401
   │
   ▼
7. Token expires after 24 hours
   │
   └─ User must login again
```

---

## 10. Database Operations Flow

```
┌─────────────────────────────────────────────────────────┐
│              DATABASE OPERATIONS FLOW                    │
└─────────────────────────────────────────────────────────┘

API Request
   │
   ▼
┌─────────────────┐
│ Flask API       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ database.py     │
│ (MongoDB client)│
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│     MongoDB Atlas (Cloud)            │
│                                      │
│  Database: astrology_ai              │
│                                      │
│  ┌────────────────────────────┐    │
│  │  users collection          │    │
│  │  - _id (ObjectId)          │    │
│  │  - email (unique index)    │    │
│  │  - password (hashed)       │    │
│  │  - name                    │    │
│  │  - is_admin                │    │
│  │  - active                  │    │
│  │  - created_at              │    │
│  └────────────────────────────┘    │
│                                      │
│  ┌────────────────────────────┐    │
│  │  chats collection          │    │
│  │  - _id (ObjectId)          │    │
│  │  - user_id (index)         │    │
│  │  - question                │    │
│  │  - chart_data              │    │
│  │  - niche                   │    │
│  │  - response                │    │
│  │  - timestamp               │    │
│  └────────────────────────────┘    │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────┐
│ Return result   │
│ to API          │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ API returns     │
│ to client       │
└─────────────────┘
```

---

## 11. Error Handling Flow

```
┌─────────────────────────────────────────────────────────┐
│                  ERROR HANDLING FLOW                     │
└─────────────────────────────────────────────────────────┘

Request arrives
   │
   ▼
Try to process
   │
   ├─ Success ──► Return 200/201
   │
   └─ Error
      │
      ├─ Missing token ──► Return 401 "Token missing"
      │
      ├─ Invalid token ──► Return 401 "Invalid token"
      │
      ├─ Not admin ──► Return 403 "Admin access required"
      │
      ├─ User not found ──► Return 404 "User not found"
      │
      ├─ Bad input ──► Return 400 "Invalid input"
      │
      ├─ User exists ──► Return 400 "User already exists"
      │
      ├─ Account disabled ──► Return 403 "Account disabled"
      │
      └─ Server error ──► Return 500 "Internal server error"
```

---

## 12. Security Layers Flow

```
┌─────────────────────────────────────────────────────────┐
│                   SECURITY LAYERS                        │
└─────────────────────────────────────────────────────────┘

Request
   │
   ▼
┌─────────────────┐
│ Layer 1: CORS   │ ← Allow cross-origin requests
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Layer 2: JWT    │ ← Validate token
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Layer 3: Role   │ ← Check admin flag
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Layer 4: Active │ ← Check account status
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Layer 5: Hash   │ ← Password hashing
└────────┬────────┘
         │
         ▼
Process Request
```

---

These flowcharts show the complete request/response flow for all major operations in the system.
